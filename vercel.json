{
	"version": 2,
	"builds": [
		{
			"src": "src/index.ts",
			"use": "@vercel/node",
			"config": {
				"tsConfig": "tsconfig.vercel.json"
			}
		}
	],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "/src/index.ts"
		}
	],
	"env": {
		"NODE_ENV": "production"
	}
} 